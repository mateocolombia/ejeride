import { getStore } from "@netlify/blobs";
import crypto from "node:crypto";
const json=(x,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const clean=(v,n=180)=>String(v||"").trim().slice(0,n);
const adminOK=req=>String(req.headers.get("x-ejeride-pin")||"")===String(process.env.EJERIDE_ADMIN_PIN||"__none__");
export default async(request)=>{const trips=getStore({name:"ejeride-trips",consistency:"strong"});const bookings=getStore({name:"ejeride-bookings",consistency:"strong"});const url=new URL(request.url);if(request.method==="GET"&&url.searchParams.get("token")){const token=clean(url.searchParams.get("token"),80);const t=await trips.get("trip/"+token,{type:"json",consistency:"strong"});if(!t)return json({error:"Not found"},404);if(t.completedAt&&Date.now()-new Date(t.completedAt).getTime()>86400000)return json({error:"Expired"},410);return json({token:t.token,status:t.status,pickupLabel:t.pickupLabel,pickupLat:t.pickupLat,pickupLng:t.pickupLng,driverLat:t.status==="completed"?null:t.driverLat,driverLng:t.status==="completed"?null:t.driverLng,driverUpdatedAt:t.driverUpdatedAt,createdAt:t.createdAt,completedAt:t.completedAt})}if(request.method==="GET"&&url.searchParams.get("admin")==="1"){if(!adminOK(request))return json({error:"PIN incorrect"},401);const list=await trips.list({prefix:"trip/"});const rows=[];for(const b of list.blobs.slice(-100)){const x=await trips.get(b.key,{type:"json",consistency:"strong"});if(x)rows.push(x)}rows.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));return json({trips:rows})}if(request.method!=="POST")return json({error:"Method not allowed"},405);if(!adminOK(request))return json({error:"PIN incorrect"},401);let b;try{b=await request.json()}catch{return json({error:"Invalid JSON"},400)}const action=b.action;if(action==="create"){let source=null;if(b.bookingId)source=await bookings.get("booking/"+clean(b.bookingId,40),{type:"json",consistency:"strong"});const token=crypto.randomBytes(16).toString("hex");const now=new Date().toISOString();const trip={token,bookingId:source?.id||null,customerName:clean(source?.name||b.customerName,80),customerPhone:clean(source?.phone||b.customerPhone,40),pickupLabel:clean(source?.pickup||b.pickupLabel,180),pickupLat:Number.isFinite(Number(source?.pickupLat??b.pickupLat))?Number(source?.pickupLat??b.pickupLat):null,pickupLng:Number.isFinite(Number(source?.pickupLng??b.pickupLng))?Number(source?.pickupLng??b.pickupLng):null,status:"en_route",driverLat:null,driverLng:null,driverAccuracy:null,driverUpdatedAt:null,createdAt:now,updatedAt:now,completedAt:null};await trips.setJSON("trip/"+token,trip);if(source){source.status="tracking_created";source.tripToken=token;await bookings.setJSON("booking/"+source.id,source)}return json({trip})}const token=clean(b.token,80);if(!token)return json({error:"Missing token"},400);const trip=await trips.get("trip/"+token,{type:"json",consistency:"strong"});if(!trip)return json({error:"Not found"},404);if(action==="location"){const lat=Number(b.lat),lng=Number(b.lng);if(!Number.isFinite(lat)||!Number.isFinite(lng)||Math.abs(lat)>90||Math.abs(lng)>180)return json({error:"Invalid location"},400);trip.driverLat=lat;trip.driverLng=lng;trip.driverAccuracy=Number.isFinite(Number(b.accuracy))?Number(b.accuracy):null;trip.driverHeading=Number.isFinite(Number(b.heading))?Number(b.heading):null;trip.driverSpeed=Number.isFinite(Number(b.speed))?Number(b.speed):null;trip.driverUpdatedAt=new Date().toISOString();trip.updatedAt=trip.driverUpdatedAt}else if(action==="status"){const allowed=new Set(["en_route","arrived","completed","cancelled"]);if(!allowed.has(b.status))return json({error:"Invalid status"},400);trip.status=b.status;trip.updatedAt=new Date().toISOString();if(b.status==="completed"){trip.completedAt=trip.updatedAt}}else return json({error:"Invalid action"},400);await trips.setJSON("trip/"+token,trip);return json({trip})};
